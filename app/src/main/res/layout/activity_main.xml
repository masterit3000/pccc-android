<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="tu.pccc.pccc.MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:layout_marginBottom="80dp"
        android:id="@+id/txtDeviceId"
        android:layout_above="@+id/txt"
        android:layout_centerHorizontal="true"
        android:hint="MakerId id"
        android:background="@android:color/darker_gray" />

    <Button
        android:text="MakerId save"
        android:layout_width="wrap_content"
        android:layout_below="@id/txtDeviceId"
        android:layout_height="wrap_content"
        android:layout_marginBottom="14dp"
        android:id="@+id/btnDeviceId"
        android:layout_above="@+id/txt"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/txt"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PCCC Android App - Click to simulate Fire alert"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/btnSimulateFire"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:visibility="gone"
        android:text="Fire"
        android:elevation="8dp"
        android:background="@color/colorAccent"
        android:fontFamily="sans-serif"
        android:textSize="18sp"
        android:textColor="@android:color/background_light"
        android:layout_below="@+id/txt"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="26dp" />


</RelativeLayout>
